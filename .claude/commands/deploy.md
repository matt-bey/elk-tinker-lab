Deploy the current project's config files to the target Raspberry Pi using the project's deploy.sh script.

Steps:
1. Identify the active project by looking at which project directory I'm currently working in, or ask me if unclear
2. Check that scripts/deploy.sh exists in that project
3. Run the deploy script: bash projects/$ARGUMENTS/scripts/deploy.sh
4. Report the result

Project (optional, inferred from context if omitted): $ARGUMENTS
