# Scripts

Reusable scripts shared across multiple projects. If a script only applies to one project, it belongs in `projects/<name>/scripts/` instead.

## Scripts

| Script | Purpose |
|--------|---------|
| [scan-network.sh](scan-network.sh) | Find Raspberry Pi devices on the local network |
